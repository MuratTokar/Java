package day_070_mrt;

public class C197 {
    /*  Which three statements are true about the structure of a Java class?
            * (Choose three.)
            A. A public class must have a main method. // not must
            B. A class can have only one private constructors. // can be more than 1
     *      C. A method can have the same name as a field.  // true // kendi ismiyle bir field e sahip olabilir
            D. A class can have overloaded static methods. // true // sataik olabilir
     *      E. The methods are mandatory components of a class. // not mandatory // metotlar sinif icin zorunlu degil
            F. The fields need not be initialized before use. // default values true
            */
}
            // cevap= C D F