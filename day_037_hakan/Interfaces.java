package day_037_hakan;

public class Interfaces {
    /**
     * Interface (Arayüz)
     * - Multiple inheritance, normal classlarda kullanilamazdi,
     *      fakat interface lerde multiple inheritance kullanilabilir
     * - Interfaces, normal classlar gibi "extends" keywordu ile inherit edilirken
     *      interfaces, "implements" keywordu ile inherit edilir.
     *
     *   public interface Animal {
     *      // icerisindeki bütün methodlar abstarcttir
     *          - static ve default keyworde sahip methodlar haric
     *          - static, nasil classa ait ise, default keyword u de interface'e ait
     *      // methodlar, implement edilmemistir
     *   }
     *
     *   1. interface icerisindeki tüm variables, static ve final dir
     *   2. interface icerisindeki tüm methodlar abstract, yani soyuttur.
     *
     *
     * public class Animal {
     *
     * }
     *
     * public interface Pet {
     *
     * }
     *
     * public class Dog extends Animal implements Pet, x, y {
     *
     * }
     *
     *  // Java 8 ile beraber, default ve static gercek method yazilabilir
     *
     *
     *
     */


}


