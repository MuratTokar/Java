package day_069_mrt;

public class C171 {
    /*
      Which three statements are true about exception handling? (Choose three.)
      İstisna  hakkında hangi üç ifade doğrudur.

          A. Only unchecked exceptions can be rethrown.      Not:checked:compile esnasinda ki hatalardir ve handle etmeliyiz
          A.Yalnızca denetlenmeyen istisnalar yeniden atılabilir. Not:Unchecked ise runtime esnasinda hata verir.handle zorunlu degil

          B. All subclasses of the RuntimeException class are not recoverable.
          B. RuntimeException sınıfının tüm alt sınıfları

          C. The parameter in a catch block is of Throwable type. // true
          C. Bir catch bloğundaki parametre, Fırlatılabilir  tipindedir

          D. All subclasses of the RuntimeException class must be caught or declared to be thrown.
          D. RuntimeException sınıfının tüm alt sınıfları,

          E. All subclasses of the RuntimeException class are unchecked exceptions. // true
          E. RuntimeException sınıfının tüm alt sınıfları un

          F. All subclasses of the Error class are not recoverable. // true
          F. Error sınıfının tüm alt sınıfları kurtarılamaz.

// cevap=C E F










     */
}
