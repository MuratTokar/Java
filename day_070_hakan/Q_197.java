package day_070_hakan;

public class Q_197 {
    int age;
    /**
     * 197. Which three statements are true about the structure of a Java class?
     * (Choose three.)
     *      A. A public class must have a main method. // not must
     *      B. A class can have only one private constructors. // can be more than 1
     *      C. A method can have the same name as a field.  // true
     *      D. A class can have overloaded static methods. // true
     *      E. The methods are mandatory components of a class. // not mandatory
     *      F. The fields need not be initialized before use. // default values true
     */
    public static void main(String[] args) {
        int age;
      //  System.out.println(age);
    }

    public void test(){
        System.out.println(age);
    }
}
